<!--
  AUTOGENERATED STARTER: .github/copilot-instructions.md
  This repository had no existing agent guidance files when this was created.
  Please edit this file to include precise, project-specific commands and
  examples once repository sources are present.
-->

# Copilot / AI agent instructions (starter)

Purpose: give an AI coding agent the minimal, concrete context needed to be
productive in this repo. Update the sections below with repository-specific
paths and commands when you add source files.

1) Immediate repo state

- No discoverable source files or README were present when this file was
  generated. If the project contains code, list the top-level entrypoints
  (example: `package.json`, `src/`, `server.js`, `index.js`, `lib/`) here.

2) High-level architecture (what to add)

- Describe the runtime and major components (e.g., "Node.js API server in
  `src/server.js` that talks to a Postgres DB via `src/db/*`.  Frontend in
  `client/` built with React and served from `public/`).
- For each component add: primary files, how it is started, and its
  configuration location (example: `.env`, `config/*.json`).

3) Developer workflows (concrete commands)

- Install dependencies: `npm ci` (or `yarn install`) — replace with the
  repository's chosen package manager.
- Build: `npm run build` (if present).
- Run locally: `npm start` or `node src/index.js` — give the exact command.
- Tests: `npm test` (note any custom test runner or flags).
- Lint/typecheck: `npm run lint`, `npm run typecheck` (if applicable).

4) Project-specific conventions (what to document here)

- Where to add new features (e.g., `src/features/*`).
- Error-handling patterns (e.g., use `async/await` + centralized middleware in
  `src/middleware/errorHandler.js`).
- Logging conventions (file/location) and important env vars (list exact
  variable names used, e.g., `DATABASE_URL`, `JWT_SECRET`).

5) Integration points & infra

- DBs and external services: list the service, connection string env var,
  and the calling module (example: Postgres via `src/db/index.js`).
- Docker or cloud deployments: reference `Dockerfile`, `docker-compose.yml`,
  or `deploy/` manifests and the relevant CI workflow file path.

6) Examples to include (edit these with repo specifics)

- "To run the API locally: `npm ci && npm start` (runs `src/server.js`)"
- "To run unit tests: `npm test -- --runInBand` (uses Jest defined in
  `package.json`)"

7) When merging existing guidance

- If a previous `.github/copilot-instructions.md` existed, preserve any
  concrete commands, env var names, and file paths. Remove generic
  placeholders and replace them with actual files.

8) How you (the maintainer) can help the AI

- Add or update this file with the exact paths and commands mentioned above.
- Provide a short top-level `README.md` describing the big-picture
  architecture and the three most important developer commands.

---
If you'd like, I can:
- scan the repo again after you add files and update this with concrete
  commands and examples, or
- insert project-specific commands now if you tell me where the app entry
  point, tests, and build scripts live.
